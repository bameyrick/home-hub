import { WeatherTemperatureDirective } from './weather-temperature.directive';

describe('WeatherTemperatureDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherTemperatureDirective();
    expect(directive).toBeTruthy();
  });
});
