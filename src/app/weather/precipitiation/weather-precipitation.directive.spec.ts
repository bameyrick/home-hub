import { WeatherPrecipitationDirective } from './weather-precipitation.directive';

describe('WeatherPrecipitationDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherPrecipitationDirective();
    expect(directive).toBeTruthy();
  });
});
