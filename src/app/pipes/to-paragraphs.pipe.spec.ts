import { ToParagraphsPipe } from './to-paragraphs.pipe';

describe('ToParagraphsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToParagraphsPipe();
    expect(pipe).toBeTruthy();
  });
});
